[project]
name = "devplan-mcp"
version = "0.1.0"
description = "MCP server for generating development plans"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.12.4",
]
